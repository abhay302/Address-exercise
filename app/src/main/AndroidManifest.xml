<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abhay.address">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.abhay.address.controllers.splash.SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.abhay.address.controllers.display.AddressListDisplayActivity"
            android:label="Addresses"
            android:launchMode="singleTop"
            android:parentActivityName="com.example.abhay.address.controllers.splash.SplashScreen"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.example.abhay.address.controllers.change.AddOrEditAddressActivity"
            android:parentActivityName="com.example.abhay.address.controllers.display.AddressListDisplayActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>